To run project do the following:

Setup env with -------------------------------------------

uv sync
uv run bruteforce.py

If not UV -------------------------------------------

pip install pycryptodome
pip install alive-progress
python3 bruteforce.py